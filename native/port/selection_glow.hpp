#pragma once

namespace rackdroid {

/** Add the selection halo overlay to the scene. Call once, after the scene
exists, from the render thread. */
void installSelectionGlow();

} // namespace rackdroid
