#!/usr/bin/env bash
# The third_party sources (Rack v2.6.4, Oboe, and every plugin) are now
# vendored directly in this repository — no submodule init is required.
# A fresh clone builds as-is. This script is kept only as a no-op stub.
echo "third_party sources are vendored in the repo — no setup needed."
